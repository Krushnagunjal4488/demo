## Steps

1. npm install -g @angular/cli
2. Paste the path in env variable, which appear after this command:
    npm config get prefix
3. ng version

## In repo

1. ng new user-app
2. Would you like to add Angular routing? → No
3. Stylesheet format → CSS
4. cd user-app
5. ng serve